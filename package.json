{
  "name": "sdk-interactivity",
  "version": "0.3.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "npm run docs --workspaces --if-present && typedoc",
    "build": "npm run build --workspaces --if-present",
    "prettier": "npm run prettier --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "author": "Fabien Lavocat <fabien.lavocat@dolby.com>",
  "license": "Millicast",
  "devDependencies": {
      "typedoc": "^0.28.7",
      "typedoc-plugin-mdn-links": "^5.0.5",
      "typescript": "^5.8.3"
  }
}
